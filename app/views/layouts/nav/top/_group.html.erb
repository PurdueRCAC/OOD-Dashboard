<%= render partial: "layouts/nav/top/dropdown",
    locals: {
      icon_class: "fas fa-folder",
      title: group.title,
      groups: OodAppGroup.groups_for(apps: group.apps, group_by: :subcategory)
    } %> 